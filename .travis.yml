dist: trusty
sudo: false

language: node_js
node_js:
  - "10"

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

cache:
  directories:
    - ./node_modules

install:
  - yarn

script:
  - yarn test s-ng-test-utils --code-coverage --watch=false --progress=false --browsers=ChromeHeadlessCI
  - yarn build s-ng-test-utils
  - yarn test s-ng-test-utils-platform --watch=false --progress=false --browsers=ChromeHeadlessCI

after_success:
  - cat coverage/s-ng-test-utils/lcov.info | node_modules/coveralls/bin/coveralls.js

notifications:
  email: false
